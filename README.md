# KING Platform

**KING** (Knowledge Integration & Neural Gateway) — масштабируемая, модульная и отказоустойчивая платформа для централизованного управления мультимодальными ИИ-агентами в гибридной среде.

## 🎯 Основные возможности

- **Event-Driven Architecture** — асинхронная обработка событий через Kafka/RabbitMQ
- **Microservices-Oriented** — модульная архитектура с четким разделением ответственности
- **Multi-Protocol Integration** — поддержка gRPC, REST/HTTP, WebSockets, SSE, FFI
- **Pluggable LLM Adapters** — унифицированный интерфейс для подключения различных LLM-провайдеров (GigaChat, YandexGPT, OpenAI, локальные модели)
- **RAG Support** — интеграция с векторными хранилищами (Qdrant, Chroma) через gRPC
- **Observability** — логирование через OpenTelemetry, метрики в Prometheus
- **Enterprise-Ready** — контейнеризация (Docker), подготовка к Kubernetes, безопасность

## 🏗️ Архитектура

Платформа построена на принципах **Clean Architecture** и **Domain-Driven Design (DDD)**:

- **Ядро (Core)** — бизнес-логика на Python, независимая от внешних зависимостей
- **Адаптеры (Adapters)** — мосты для взаимодействия с внешними сервисами
- **Инфраструктура (Infrastructure)** — технические детали реализации (БД, очереди, логирование)

Подробное описание архитектуры см. в [Дизайн.md](Дизайн.md).

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Docker и docker-compose
- PyCharm Community Edition (рекомендуется)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd KING
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env и добавьте необходимые ключи (GigaChat credentials и т.д.)
```

5. Запустите инфраструктуру через docker-compose:
```bash
docker-compose up -d
```

6. Запустите платформу:
```bash
python -m king.main
```

## 📁 Структура проекта

```
KING/
├── king/                    # Основной пакет
│   ├── core/               # Ядро системы (доменная логика)
│   │   ├── domain/         # Доменные модели и события
│   │   ├── services/       # Бизнес-сервисы
│   │   └── ports/          # Интерфейсы (ports)
│   ├── adapters/           # Адаптеры для внешних сервисов
│   │   ├── llm/            # LLM-адаптеры (GigaChat, OpenAI и т.д.)
│   │   ├── grpc/           # gRPC-клиенты и серверы
│   │   ├── http/           # REST/HTTP-клиенты
│   │   ├── messaging/      # Kafka/RabbitMQ адаптеры
│   │   ├── websocket/      # WebSocket/SSE адаптеры
│   │   └── ffi/            # FFI-адаптеры для нативных библиотек
│   ├── infrastructure/     # Инфраструктурный слой
│   │   ├── config/         # Конфигурация
│   │   ├── logging/        # Логирование (OpenTelemetry)
│   │   ├── metrics/        # Метрики (Prometheus)
│   │   └── persistence/    # Хранилища данных
│   └── api/                # API-слой
│       ├── rest/           # REST API
│       ├── grpc/           # gRPC API
│       └── websocket/      # WebSocket API
├── config/                 # Конфигурационные файлы
│   ├── prompts/            # Шаблоны промптов (Jinja2 + YAML)
│   └── services/           # Конфигурация сервисов
├── tests/                  # Тесты
├── docker/                 # Docker-конфигурации
├── k8s/                    # Kubernetes манифесты
├── docs/                   # Документация
├── docker-compose.yml      # Локальная инфраструктура
├── requirements.txt        # Python-зависимости
└── .env.example            # Пример переменных окружения
```

## 🔌 Подключение GigaChat

### 1. Регистрация в Сбер ID

1. Перейдите на [https://developers.sber.ru/](https://developers.sber.ru/)
2. Создайте приложение и получите `client_id` и `client_secret`
3. Добавьте их в `.env`:
```env
GIGACHAT_CLIENT_ID=your_client_id
GIGACHAT_CLIENT_SECRET=your_client_secret
```

### 2. Использование

```python
from king.adapters.llm.gigachat import GigaChatAdapter
from king.core.services.llm_service import LLMService

# Инициализация адаптера
adapter = GigaChatAdapter(
    client_id=os.getenv("GIGACHAT_CLIENT_ID"),
    client_secret=os.getenv("GIGACHAT_CLIENT_SECRET")
)

# Использование через сервис
llm_service = LLMService(adapter)
response = await llm_service.generate("Привет, как дела?")
```

## 🧪 Тестирование

```bash
# Запуск всех тестов
pytest

# С покрытием
pytest --cov=king --cov-report=html

# Конкретный модуль
pytest tests/adapters/llm/test_gigachat.py
```

## 📊 Мониторинг

- **Метрики**: http://localhost:9090 (Prometheus)
- **Логи**: через OpenTelemetry (экспорт в Jaeger/CloudWatch)
- **Health Check**: http://localhost:8000/health

## 🔒 Безопасность

- Валидация входных данных через Pydantic
- Изоляция адаптеров через sandboxing
- Управление секретами через переменные окружения и Vault-совместимую абстракцию
- OAuth2 для аутентификации внешних сервисов

## 🛠️ Разработка

См. [Список заглушек.md](Список%20заглушек.md) для списка задач и заглушек.

### Добавление нового LLM-адаптера

1. Создайте класс, наследующий `AbstractLLMClient` в `king/adapters/llm/`
2. Реализуйте методы `generate()`, `stream()`, `get_embeddings()`
3. Добавьте конфигурацию в `config/prompts/`
4. Зарегистрируйте адаптер в `king/core/services/llm_service.py`

## 📚 Документация

- Readme.md

## 🤝 Вклад

Проект находится в активной разработке разработчицей Viktoriya Gonchar

## 📄 Лицензия

MIT

